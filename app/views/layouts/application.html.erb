<!DOCTYPE html>
<html>
<head>
  <title>OCTF</title>
	<link href='http://fonts.googleapis.com/css?family=Source+Sans+Pro:300,400,700' rel='stylesheet' type='text/css'>
  	<%= stylesheet_link_tag "application" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
</head>
<body>
	
		
	<% if current_user %>
	
		<header class="header-wrap">
			<nav class="navbar group">
				<h1 class="logo">OCTF (beta)</h1>
			
				<ul class="nav-list">
					<li class="nav-list-item"><a href="<%= athletes_url %>">All Athletes</a></li>
					<li class="nav-list-item"><a href="<%= records_url %>">School Records</a></li>
					<li class="nav-list-item"><a href="#"><%= current_user.username.upcase %></a></li>
					<li class="nav-list-item"><%= button_to "Sign Out", session_url, method: :delete %></li>
					<% if current_user.admin? %>
						<li class="nav-list-item"><%= link_to "Add Athlete", new_athlete_url %></li>
					<% end %>
				</ul>
			</nav>
		
		</header>
	
	<% end %>
	
	<div id="container">

		<%= flash[:notice] %>
		<%= flash[:errors] %>
		<%= yield %>
	
		<% if current_user %>

			<% if current_user.type == "Admin" %>
				<%= debug params %>
			<% end %>
	
		
		<% end %>
	
	</div>
	
	<% if current_user %>
		<footer>
			Why hello there.
			This is just a small side project of mine and it's still under construction, but I have a LOT more planned :)
		</footer>
		<br>
	<% end %>
	
</body>
</html>
